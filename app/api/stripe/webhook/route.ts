// TODO (Week 7): Stripe webhook handler
// See CLAUDE.md — Stripe Plans section for full spec.

export async function POST(request: Request) {
  return new Response('Stripe webhook stub — implement in Week 7', { status: 200 })
}
